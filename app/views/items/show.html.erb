<%= stylesheet_link_tag "show", "data-turbo-track": "reload" %>

<body>
  <div class="container">
    <h1><%= @item.name %></h1>
    <p><%= @item.description %></p>
    <p>Cantidad: <%= @item.quantity %></p>

    <div class="actions">
      <%= link_to 'Editar', edit_item_path(@item), class: 'edit-link' %>
      <%= link_to 'Volver', items_path, class: 'back-link' %>
    </div>
  </div>
</body>
